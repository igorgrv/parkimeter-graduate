spring:
  application.name: ParkingMeterMonitor
  banner.location: static/banner.txt
  main:
    allow-circular-references: true
  data:
    mongodb:
        uri: mongodb://localhost/parking-meter
        port: 27017
        username:
        password:
